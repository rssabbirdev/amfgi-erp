import EmployeePortalLayout from '@/components/me/EmployeePortalLayout';

export default function MeLayout({ children }: { children: React.ReactNode }) {
  return <EmployeePortalLayout>{children}</EmployeePortalLayout>;
}
